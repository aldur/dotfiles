--- a/internal/cmd/cmd_check_required.go
+++ b/internal/cmd/cmd_check_required.go
@@ -57,13 +57,9 @@ func cmdCheckRequiredAction(_ Env, args []string, config *Config) error {
 			return nil
 		}
 
-		// Security validation: no parent traversal
-		if strings.Contains(relPath, "..") {
-			fmt.Printf("log_error %s;\n", BashEscape("require_allowed: path must not contain '..': "+relPath))
-			fmt.Println("exit 1;")
-			return nil
-		}
-
+		// Parent inputs use the same resolution as allowRequiredFiles.
+		// Approval records remain keyed by the envrc path and file hash;
+		// input paths are never used as paths inside the approval database.
 		absPath := filepath.Join(envrcDir, relPath)
 		hash, err := fileHash(absPath)
 		if err != nil {
--- a/test/stdlib.bash
+++ b/test/stdlib.bash
@@ -229,17 +229,17 @@ test_name require_allowed_security
   [[ $result -eq 1 ]]
   [[ "${output#*'path must be relative'}" != "$output" ]]
 
-  # Test that parent traversal paths are rejected
-  output="$(require_allowed ../etc/passwd 2>&1)"
+  # Parent inputs still need explicit approval before activation.
+  output="$(require_allowed ../unapproved-parent-input 2>&1)"
   result=$?
-  [[ $result -eq 1 ]]
-  [[ "${output#*'must not contain'}" != "$output" ]]
+  [[ $result -eq 0 ]]
+  [[ "${output#*'requires approval'}" != "$output" ]]
 
-  # Test that paths with .. in the middle are rejected
-  output="$(require_allowed foo/../bar 2>&1)"
+  # Normalized relative inputs follow the same approval rule.
+  output="$(require_allowed foo/../unapproved-input 2>&1)"
   result=$?
-  [[ $result -eq 1 ]]
-  [[ "${output#*'must not contain'}" != "$output" ]]
+  [[ $result -eq 0 ]]
+  [[ "${output#*'requires approval'}" != "$output" ]]
 )
 
 # test strict_env and unstrict_env
